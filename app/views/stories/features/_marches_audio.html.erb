<div class="audio-container card card-body bg-light">
  <%= sanitize text %>
  <audio controls preload="none">
    <% audio = "#{PRIVATE["media_path"]}/audio/bandhistory/stories/marches/#{mp3_path}" %>
    <source type="audio/mpeg" src="<%= audio %>">
    <p>Your browser doesn't support HTML5 audio. Here is
       a <a href="<%= audio %>">link to the audio</a> instead.</p>
  </audio>
</div>
