<% @title = "Correspondence & Papers" %>

<h1>Correspondence & Papers</h1>

<div class="row">
  <div class="col-md-9">
    <p class="lead">
      There is a wealth of information about the band in the Archives left by a paper trail of correspondence, records, bills, and more. These documents can be found in Chancellor records, Board of Regents minutes, military records, and collections left behind by past band directors.
    </p>
    <p>
      The vast majority of the records in Archives have not yet been digitized, but several documents are available for searching and browsing on the website at this time.
    </p>
  </div>
  <div class="col-md-3">
    <%= image_tag "correspondence.jpg", alt: "First page of letter from Professor of Military and Tactics" %>
  </div>
</div>

<div class="row">
  <div class="col-md-6">

    <h2 class="text-center">Search</h2>

    <%= form_tag prefix_path(route_path), method: "get",
      class: "input-group" do %>
      <!-- search box -->
      <%= text_field_tag(:q, params[:q], :placeholder => t("search.placeholder", default: "Search for..."), :class => "form-control") %>
      <!-- add existing search parameters to query -->
      <%= render_overridable("items", "hidden_fields") %>
      <!-- submit -->
      <span class="input-group-btn">
        <%= submit_tag t("search.submit", default: "Search"), class: "btn btn-primary", name: nil, data: { disable_with: false } %>
      </span>
    <% end %>
    <%= render_overridable("items", "help") %>
  </div>
  <div class="col-md-6">
    <h2 class="text-center">Browse</h2>
    <div class="filter-buttons">
      <%= link_to "Browse All Papers", correspondence_search_path, class: "btn btn-primary btn-filter", role: "button" %>
    </div>
  </div>
</div>
